export class Aatish {
    constructor() {}
}

/**
 * Aatish
 *
 * const app = new Aatish({
 *  connect(){
 *
 * }
 * })
 *
 * app.use(middleware())
 *
 * app.on("event", (data, user,cb) => {
 *
 *  return cb()
 * })
 *
 * app.emit()
 */
